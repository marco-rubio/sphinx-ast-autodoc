[tox]
envlist =
  py{38, 39},

[testenv]
whitelist_externals = make
basepython =
  py38: python3.8
  py39: python3.9

setenv =
  PYTHONPATH={toxinidir}
  PYTHONDONTWRITEBYTECODE=1
  PYTHONWARNINGS=once

deps =
  pudb
  pytest
  pytest-cov
  pytest-mock
  pytest-pudb

commands = make coverage
